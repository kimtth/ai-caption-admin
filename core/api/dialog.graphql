type Count {
    count: Int
}

type CountWithTotal {
    count: Int
    totalCount: Int
}

type CountDay {
    day: String
    count: Int
}

extend type Query {
    audioCnt: CountWithTotal
    userCnt: Count
    customCnt: CountWithTotal
    channelCnt: Count
    trafficCnt: [CountDay]
}